create-dirs:
  script:
    - echo create dirs!
    - cat config/navi-music.yml | yq "del(.[].[].cover_art) | del(.[].[].[].[])" > dirs.yaml

download-m4a-s:
  script:
    - echo download-m4a-s!

download-cover-art:
  script:
    -echo dl-cover!


